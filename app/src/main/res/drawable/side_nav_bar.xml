<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    //側邊欄背景顏色
    //centerColor 就是中間的顏色
    //endColor 就是最下面的顏色
    //startColor 就是最上面的顏色
    //angle 設定角度centerColor 就是中間的顏色
    //endColor 就是最下面的顏色
    //startColor 就是最上面的顏色
    //angle 設定角度
    <gradient
        android:angle="135"
        android:centerColor="#ffffff"
        android:endColor="#dddddd"
        android:startColor="#009999"
        android:type="linear" />
</shape>